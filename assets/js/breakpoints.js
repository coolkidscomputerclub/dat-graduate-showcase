const ALPHA = 480;
const BETA = 640;
const GAMMA = 800;
const DELTA = 960;
const EPSILON = 1280;
const ZETA = 1440;

const rootFontSize = 16;

const alpha = {
  px: ALPHA,
  em: ALPHA / rootFontSize,
};

const beta = {
  px: BETA,
  em: BETA / rootFontSize,
};

const gamma = {
  px: GAMMA,
  em: GAMMA / rootFontSize,
};

const delta = {
  px: DELTA,
  em: DELTA / rootFontSize,
};

const epsilon = {
  px: EPSILON,
  em: EPSILON / rootFontSize,
};

const zeta = {
  px: ZETA,
  em: ZETA / rootFontSize,
};

module.exports = {
  alpha,
  beta,
  gamma,
  delta,
  epsilon,
  zeta,
};
